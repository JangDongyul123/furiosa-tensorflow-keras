미니콘다 설치 후

conda env list

conda create -n py311 python=3.11

vs 코드 설치

ai 프레임워크 대결구도 pytorch vs tensorflow2(keras)

pythorch 생태계 윈

batch를 쓰는 이유

1. 메모리 부족(input layer가 x가 10만, output layer가 y도 10만이면 10만의 제곱이라서 오버플로우)

2. 과적합 방지(데이터 통째로 돌리면 과적합)

3. 실질적 훈련수 증가(데이터를 반으로 자르면 epoch=1이어도 2번 훈련)

질문1.
미니콘다 설치 후
activate py311(가상환경이름) 와 conda.bat activate py311(가상환경이름) 차이?

질문2.
같은 가상환경이면
(py311) C:\Users\Admin>pip install tensorflow
와
(py311) C:>pip install tensorflow
가
같나?


tensorflow2 -> tensorflow1 -> pytorch 순서로 가르치시겠다고 하심

윤영선 선생님


y = wx+b 에서 w 가중치 기울기를 조정하는 걸 역전파라고 한다.
최적의 w를 찾는 게 학습이다.

예) x가 1일 때 y가 1이고 x가 2일 때 y가 2인걸 아는 건 역전파 덕분(기울기 조정)

x가 고양이가 물고기를 보면
y가 고양이가 침흘리는 거
중간에 굉장히 많은 뉴런(신경망)이 있고, 그 신경망을 연결하는 선만큼 y = ax+b가 있음
(그것도 순차적으로 신경망들이 이어져있다. 눈 - o layer1 - o - o - o layer4-입)



질문3. 최적의 b(편향)를 찾는 것도 역전파인가?

ㅇㅇ w와 b가 같이 변경된다.


최상단 INPUT LAYER, 최하단 OUTPUT LAYER, 그 사이의 신경망층을 hidden layer

사람이 관여하는 건 INPUT LAYER, OUTPUT LAYER



훈련은 많이할수록 좋지만, 너무 많이해도 과적합이 생김
LOSS='MSE'는 -가 생길 수가 없고 나중에 설명해준다고 하셨음 MSE 수식을


학습 데이터를 더 늘리면 조금 더 적은 EPOCHS로도 된다.



챗지피 개발자들도 hidden layer가 어떻게 되어있는지 모른다. 그저 loss를 잘 구해서 모델을 잘 만든 것이다.